# 0.8.0-beta.27 — Rollout verification

The live beta.26 check found that readiness counted every inventory item as
requested equipment while counting only included meters as ready. Both counts
now use the included equipment shown on Devices, including the home battery.
Excluded inventory no longer inflates the denominator.

The test website's database migration, functions and portal deployed successfully
at `cdd0ff1`; the beta.26 release workflow succeeded at `f9c6af4`. The running HA
installation uses that test environment. A fresh exchange received schema-7
device commands and advanced electrical and thermal history. Existing local
control permissions remained off.

334 Python tests and 10 frontend tests pass. A same-value save through the
supported panel API exercises persistence without changing device setup.
Final restart and live verification are recorded in the Phase 5 rollout report.

This release does not select a new EV meter, alter website inclusion decisions,
enable hardware control, or promote the website to production.
